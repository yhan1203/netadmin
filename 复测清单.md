# 复测清单 — P001-netadmin

> 修改代码后逐项验证，确保没把原来好的搞坏。

## 核心功能回归

- [ ] CLI 能正常启动：`netadmin --help`
- [ ] 所有命令的 `--help` 不报错：`netadmin scan --help`、`netadmin backup --help` 等
- [ ] 测试全部通过：`pytest`
- [ ] 厂商命令映射正确：华为 `display vlan` / 思科 `show vlan brief`
- [ ] demo 脚本正常输出：`python3 demo.py`

## 边界情况

- [ ] 配置文件不存在时不崩溃
- [ ] VLAN 提取容忍缩进
- [ ] SNMP location 兼容 `snmp-server location` 和 `snmp-agent sys-info location` 两种格式
- [ ] STP 模式匹配 `rapid-pvst` 这类复合模式名
- [ ] 思科 apply 时 `spanning-tree mode rapid-pvst` 能正确下发
- [ ] 华为 apply 的 `stp mode` 命令正常
- [ ] 备份 SQLite 数据库空时不崩溃

## 环境检查

- [ ] 测试全部通过：`pytest`
- [ ] 覆盖率无倒退（目标：纯逻辑模块 > 70%）
- [ ] 无敏感信息泄露（密码/token 硬编码）