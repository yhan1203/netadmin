# 复测清单 — P001-netadmin

> 修改代码后逐项验证，确保没把原来好的搞坏。

## 核心功能回归

- [x] CLI 能正常启动：`netadmin --help`
- [x] 所有命令的 `--help` 不报错：`netadmin scan --help`、`netadmin backup --help` 等
- [x] 测试全部通过：`pytest`  (82 passed)
- [x] 厂商命令映射正确：华为 `display vlan` / 思科 `show vlan brief`
- [x] demo 脚本正常输出：`python3 demo.py`

## 边界情况

- [x] 配置文件不存在时不崩溃
- [x] VLAN 提取容忍缩进
- [x] SNMP location 兼容 `snmp-server location` 和 `snmp-agent sys-info location` 两种格式
- [x] STP 模式匹配 `rapid-pvst` 这类复合模式名
- [x] 思科 apply 时 `spanning-tree mode rapid-pvst` 能正确下发
- [x] 华为 apply 的 `stp mode` 命令正常
- [x] 备份 SQLite 数据库空时不崩溃
- [x] learn 输出文件写入 YAML 而非 NamedTuple
- [x] VLAN name 提取使用 m.end() 而非 config.index() 避免串位
- [x] SSH 版本检查使用 vendor 参数而非在 config 文本中搜 "huawei"
- [x] logging 检查不会误判 "login" 为 logging
- [x] scan 无参数时显示默认网段提示
- [x] VLAN save 操作在 with 块内（不会用已断开连接）
- [x] 华为中文 locale 输出可解析
- [x] BackupManager GC 时自动关闭 DB 连接
- [x] apply 模板格式校验（vlans/interfaces/ntp 类型错误不崩溃）
- [x] 所有 --username/-u 选项有一致的 "用户名" 帮助文本
- [x] parse_interval 支持 30m/1h/daily/hourly/原生 crontab 五种格式
- [x] BackupScheduler add/remove/toggle/list/run 全部正常工作
- [x] run_scheduled_backups 无设备时不崩溃
- [x] schedule CLI 命令组注册正确，--help 不报错
- [x] NotificationConfig 从空 dict 构造不崩溃
- [x] AlertNotifier 未配置时 send_alert 静默跳过
- [x] health_alert 仅 HIGH 状态触发告警，正常状态不触发
- [x] audit_alert 低分触发告警，满分不触发
- [x] check --notify 和 audit --notify 参数注册正确
- [x] config.yaml notifications 节格式兼容 Telegram/钉钉
- [x] Web 仪表盘 4 页面全部返回 200（dashboard/backups/schedules/health）
- [x] 不存在的备份 ID 返回 404
- [x] 不存在的备份对比返回 404
- [x] netadmin web 命令注册正确
- [x] 测试 108/108 全通过

## 环境检查

- [x] 测试全部通过：`pytest` (82 passed)
- [x] 覆盖率无倒退（目标：纯逻辑模块 > 70%）
- [x] 无敏感信息泄露（密码/token 硬编码）
- [x] ruff 零告警